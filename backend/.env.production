PORT=8000
DEBUG=False
SECRET_KEY=your-super-secret-key-generate-new-one-with-strong-random-string-min-50-chars
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com,yourdomain.hostinger.com

# Database Configuration for Hostinger MySQL
DATABASE_URL=mysql://dbuser:dbpassword@localhost:3306/dbname

# AWS S3 or use local storage (optional for media)
USE_LOCAL_STORAGE=True

# Security
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True

# Email Configuration (for Hostinger)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.hostinger.com
EMAIL_PORT=465
EMAIL_USE_SSL=True
EMAIL_HOST_USER=your-email@yourdomain.com
EMAIL_HOST_PASSWORD=your-email-password

# Optional: Stripe (if using payment)
STRIPE_SECRET_KEY=sk_live_your_stripe_secret
STRIPE_PUBLISHABLE_KEY=pk_live_your_stripe_public

# Optional: AWS S3 (if not using local storage)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_STORAGE_BUCKET_NAME=
# AWS_S3_REGION_NAME=ap-south-1
